import * as React from "react";

const SiteHeader: React.SFC<{}> = ({}) => {
  return (
    <div className="site-header">
      <h1>CloudFront Invalidator</h1>
    </div>
  )
}

export default SiteHeader
